<?php

declare(strict_types=1);
function binaryOp() {
    $x += $y;
    $x -= $y;
    $x *= $y;
    $x /= $y;
    $x %= $y;
    $x &= $y;
    $x |= $y;
    $x ^= $y;
    $x <<= $y;
    $x >>= $y;
    $x .= $y;
    $x ??= $y;
}
