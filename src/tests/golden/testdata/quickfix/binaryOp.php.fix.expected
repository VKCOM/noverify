<?php

function binaryOp() {
    $x += $y;
    $x -= $y;
    $x *= $y;
    $x /= $y;
    $x %= $y;
    $x &= $y;
    $x |= $y;
    $x ^= $y;
    $x <<= $y;
    $x >>= $y;
    $x .= $y;
    $x ??= $y;
}